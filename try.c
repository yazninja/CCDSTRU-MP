#include <stdio.h>
#include <stdlib.h>

struct position{
	int a, b; //values 1-4
};

typedef struct position Player;

int main()
{
	int turn, over; // 1(true) or 0(false)
	
	Player Ord,Cha,Free;
  
  
  return 0;
}

